using UnityEngine;

namespace Atomic.Core
{
    public class EntityMBManager : MonoBehaviour
    {
        [SerializeField]
        private EntitySOManager _entitySOManager;

        // Start is called before the first frame update
        private void Start()
        {

        }

        // Update is called once per frame
        private void Update()
        {

        }
    }

}
